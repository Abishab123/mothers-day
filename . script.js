// The JavaScript file to handle interactions (if needed in the future)
document.getElementById('checkbox').addEventListener('change', function() {
    // This handles the checkbox state change for the letter animation
});
